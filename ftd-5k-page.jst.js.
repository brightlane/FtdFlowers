const fs = require("fs");
const path = require("path");

// --------------------
// CONFIG
// --------------------
const BASE_URL = "https://brightlane.github.io/FtdFlowers/";
const SLUG = "same-day-roses-order-flowers-online-new-york";

const OUTPUT_DIR = path.join(__dirname, "output", "flower-pages");
if (!fs.existsSync(OUTPUT_DIR)) {
  fs.mkdirSync(OUTPUT_DIR, { recursive: true });
}

const FILE_PATH = path.join(OUTPUT_DIR, `${SLUG}.html`);
const URL = `${BASE_URL}${SLUG}.html`;

// --------------------
// HTML GENERATOR
// --------------------
function generate5kHtml() {

  const title = "Same Day Roses Order Flowers Online New York — SEO Guide";

  const keywords = [
    "order flowers online",
    "same day roses",
    "flower delivery",
    "roses NYC",
    "buy roses online",
    "send flowers New York",
    "FTD flowers",
    "same day flower delivery NYC"
  ].join(", ");

  const description = `
Order same day roses online in New York City with fast local florist delivery.
Learn how to buy roses online, delivery timing, care tips, and best practices.
`;

  const body = `
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>${title}</title>
  <meta name="description" content="${description.trim()}">
  <meta name="keywords" content="${keywords}">
  <meta name="robots" content="index, follow">
  <link rel="canonical" href="${URL}">
</head>

<body>

<header>
  <h1>Same Day Roses in New York City</h1>
  <p>Order flowers online with same-day delivery across NYC.</p>

  <a href="https://www.floristone.com/index.cfm?source_id=aff&AffiliateID=2013017799">
    🌹 Order Roses Now
  </a>
</header>

<main>

<section>
  <h2>How to order same-day roses online</h2>
  <p>
    Customers in New York City use online florists to send roses for birthdays,
    anniversaries, sympathy, and romantic occasions. Same-day delivery depends
    on ordering before the local cutoff time.
  </p>
</section>

<section>
  <h2>Why local florists matter</h2>
  <p>
    Local NYC florists ensure fresh-cut roses are arranged and delivered quickly.
    This improves freshness and reduces shipping delays.
  </p>
</section>

<section>
  <h2>Care tips for roses</h2>
  <p>
    Trim stems, change water every 2–3 days, and keep away from heat sources.
  </p>
</section>

</main>

<footer>
  <p>© 2026 BrightLane Flowers | Affiliate Partner Network</p>
</footer>

</body>
</html>
`;

  return body;
}

// --------------------
// WRITE FILE
// --------------------
const html = generate5kHtml();
fs.writeFileSync(FILE_PATH, html);

console.log("✅ Page created:", FILE_PATH);

// --------------------
// OPTIONAL: CREATE INDEX LINK FILE
// --------------------
const indexPath = path.join(OUTPUT_DIR, "index-links.html");

const indexLink = `
<li>
  <a href="${SLUG}.html">${SLUG.replace(/-/g, " ")}</a>
</li>
`;

fs.writeFileSync(indexPath, indexLink);

console.log("✅ Index link created");

// --------------------
// OUTPUT URL FOR DEPLOYMENT CHECK
// --------------------
console.log("🌐 LIVE URL (after deploy):");
console.log(URL);
