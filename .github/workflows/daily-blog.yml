name: Daily Blog Generator
on:
  schedule:
    - cron: "0 9 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Git config
        run: |
          git config user.name "auto-blog-bot"
          git config user.email "bot@brightlane.io"

      - name: Pull latest
        run: |
          git fetch origin main
          git reset --hard origin/main

      - name: Run daily blog generator
        run: node generate-daily-blog.js

      - name: Run multilingual Bing blast (2000 pages, 5 languages)
        run: node ftd_bing_blast.js

      - name: Commit and push
        run: |
          git add .
          git diff --staged --quiet && echo "Nothing to commit" && exit 0
          git commit -m "Daily update + Bing blast [$(date +'%Y-%m-%d')]"
          git fetch origin main
          if git rebase -X theirs origin/main; then
            git push origin main && exit 0
          fi
          git rebase --abort || true
          git reset --hard origin/main
          git add .
          git diff --staged --quiet && exit 0
          git commit -m "Daily update (retry) [$(date +'%Y-%m-%d')]"
          git push origin main
